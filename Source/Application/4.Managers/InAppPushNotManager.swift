//
//  InAppPushNotManager.swift
//  SmartApp
//
//  Created by Ricardo Santos on 27/08/2024.
//

/*
 import Foundation

 struct SomeDataType {
     let id:
     let txt
     let title
     let sybtitke
     let soud
     tet number
     let displayAt: Date
 }

 class InAppPushNotManager {

     var mintTimeToHandle: TimeInterval = 1000000

 timert to call

     init() {
         // Observer to know data was received
         handleData(some: nil)
     }

     static var shared = InAppPushNotManager()

     func markAsDisplayed(id: String) {
         1 -clean record from db (was handled)
         2 - wanr the api that the user was display with the ina pp Notification
     }

     func handleData(list: [SomeDataType]) {
         // 1 - we have data to display
         list.forEach {
             if time to be displayed > mintTimeToHandle {
                 1 - create the in app notiica with the time to appear
                 2 - create some flag on DB telling we intendo do display in app not
                     NotificationCenter.shared.present(

             }
         }
     }

     func fecthData() {
         // 1 - api call
         woud return an [SomeDataType]
         // 2 data stores
     }
 }*/
